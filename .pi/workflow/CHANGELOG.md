# Pi Workflow Distribution Changelog

This changelog tracks changes relevant to the distributed `.pi/workflow/` package.

## [Unreleased]

### Added

- Distributed workflow README.
- Distributed workflow changelog.
- Isolated scripts and task storage under `.pi/workflow/`.

### Changed

- Workflow support is distributed inside `.pi/workflow/` instead of root-level `scripts/` and `tasks/`.
