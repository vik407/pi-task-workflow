# FXT-0001 Implementation

## Changes made

Created a real Node.js fixture project with calculator source code, tests, and task artifacts.

## Files changed

- `package.json` — defines `npm test`.
- `src/calculator.mjs` — implements calculator behavior.
- `test/calculator.test.mjs` — validates calculator behavior.
- `tasks/FXT-0001/*` — stores task workflow artifacts.

## Notes

This fixture proves workflow mechanics against real executable project code, not only synthetic root artifacts.
